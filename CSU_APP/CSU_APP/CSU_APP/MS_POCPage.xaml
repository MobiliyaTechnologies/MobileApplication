<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:MS_POC" 
             x:Class="CSU_APP.MS_POCPage">
  <StackLayout Spacing="20" Padding="40"
                         >
    <Label Text="Login Page"/>
    <Entry Placeholder="Username"
           Text="{Binding Username}" x:Name="Uname"/>
    <Entry Placeholder="Password"
           Text="{Binding Password}"
           IsPassword="true" x:Name="pword"/>
    <Button Text="Login" TextColor="White" x:Name="performLogin"
            BackgroundColor="#77D065"
            Command="{Binding LoginCommand}"/>



    <ActivityIndicator
                         IsVisible="{Binding IsBusy}"
                         IsRunning="{Binding IsBusy}"
                         Color="Black" />
  </StackLayout>

</ContentPage>